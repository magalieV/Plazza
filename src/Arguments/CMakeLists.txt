set(PATH_H
        inc/Arguments/)
set(PATH_S
        src/Arguments/)

set(SOURCES
        ${PATH_S}Arguments.cpp
        )

set(HEADERS
        ${PATH_H}Arguments.hpp
        )

set(ARGS_FILES
        ${SOURCES}
        ${HEADERS}
        )