set(PATH_H
        inc/Tools/)
set(PATH_S
        src/Tools/)

set(SOURCES
        ${PATH_S}Tools.cpp
        )

set(HEADERS
        ${PATH_H}Tools.hpp
        )

set(TOOLS_FILES
        ${SOURCES}
        ${HEADERS}
        )