set(PATH_H
        inc/Pizza/)
set(PATH_S
        src/Pizza/)

set(SOURCES
        ${PATH_S}Pizza.cpp
        )

set(HEADERS
        ${PATH_H}Pizza.hpp
        ${PATH_H}PizzaMap.hpp
        )

set(PIZZA_FILES
        ${SOURCES}
        ${HEADERS}
        )